services:
  redis:
    image: redis:8.8
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    # command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data

volumes:
  redis_data:
