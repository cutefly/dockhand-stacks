services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vw.club012.com"
    ports:
      - 8000:80
    volumes:
      - vw_data:/data/

volumes:
  vw_data:
